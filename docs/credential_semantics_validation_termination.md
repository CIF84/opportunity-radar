# SPEC-020 Credential Semantics — Terminal Exploratory Result

Status: `TERMINATED_SOURCE_DECAY_CONFOUNDED` (human decision DR-025, 2026-09-15).
This report is derived from the repository-safe terminal receipt. It is not a
completed validation report and does not replace the immutable preparation
evidence or the private append-only judgment log.

## What was preserved

Preparation `spec020-credential-preparation-20260914-v3` froze 50 reviews,
their deterministic order, and ten same-stratum reserves against a verified
144-case source population. Twenty reviews were recorded. Their private notes,
original identities, frozen evidence, and zero replacement records remain
unchanged. No remaining reviews were adjudicated.

## Sanitized exploratory observations

```text
reviewed / frozen             20 / 50
INVALID_OR_STALE_EVIDENCE    12 / 20
substantively interpretable   8 / 20
HARD_CREDENTIAL               8 / 8 interpretable
EXPERIENCE_PLAUSIBLY_SUBSTITUTES  6 / 8 interpretable
DEGREE_GAP_DECISIVE           2 / 8 interpretable
```

The page-unavailability notes indicate source-decay confounding; they do not
independently establish an ingestion defect in the frozen qualification text.
The eight interpretable cases all concerned hard credential wording, so
preferred, equivalent-experience, generic, and ambiguous credential semantics
remain unvalidated. Human reasoning repeatedly distinguished independent
capability/domain gaps from the degree gap itself.

These proportions are not population estimates, validated current-rule
precision, or a basis for runtime rejection or semantic allocation. The
employer-concentrated balanced sample also cannot be used as prevalence.

## Evidence and policy boundary

The sanitized canonical receipt is
`output/credential_semantics_validation/spec020-credential-preparation-20260914-v3/aggregate_termination.json`.
It preserves protocol, source-population, selection, frozen-packet, operational
SQLite, and private judgment-log hashes without exposing vacancy identities
or human notes. Detailed evidence remains private/local.

No predeclared final 50-case gate, counterfactual performance, or employer-
population-weighted estimate was calculated. SPEC-019 stretch behavior,
candidate state, ranking, recommendation, semantic allocation, and runtime
credential policy remain unchanged. A further experiment requires a new
explicitly approved protocol and identity; this packet must not be resumed.

Offline validation after termination: 326 passed, 2 expected private-evidence
skips, 22 live tests deselected, and `git diff --check` passed. No external
semantic or live-source call was made.
